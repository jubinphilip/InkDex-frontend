import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "InkDex — Your Document Intelligence Platform",
  description:
    "Upload PDFs, index their knowledge, and ask questions using AI-powered retrieval.",
  keywords: ["document AI", "RAG", "PDF search", "knowledge base"],
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
